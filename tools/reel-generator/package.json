{
  "name": "soulcraft-reel-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local render engine for platform-ready vertical reels (The Art of Soulcraft). Not deployed — run locally / in CI.",
  "bin": { "soulcraft-reel": "src/cli.mjs" },
  "scripts": { "reel": "node src/cli.mjs" },
  "dependencies": {
    "playwright-core": "^1.47.0"
  },
  "optionalDependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0"
  }
}
